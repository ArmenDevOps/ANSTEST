- hosts: web
  tasks:
  - name: Install the latest version of Apache
    yum:
      name: httpd
      state: latest

  - name: Make sure a service is running
    systemd:
      name: httpd
      state: started
      enabled: yes
